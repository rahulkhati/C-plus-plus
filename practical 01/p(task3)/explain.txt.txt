1.As we are using a char datatype so if even we input abc or 123 as input the compiler 
2.will store only ! char from the input data as it can store a character in the 8 bit 
3.memory which is allocated type.
4.We can use get line command instead of this to get full string as a input.
